spring:
  cloud:
    consul: 
      host: localhost
      port: 8500
      discovery:
        enabled: true
        instance-id: application
  application:
    name: democonsul
    
server:
  port: 8055

        
